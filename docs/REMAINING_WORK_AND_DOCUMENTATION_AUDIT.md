# 現行修正課題・ドキュメント整理表

更新日: 2026-07-22  
対象: `0.5.2-dev` の現行ワークツリー

## 1. この文書の役割

初回個人配布までに残っている実装修正、追加検証、ドキュメント整理を一か所で管理する。
詳細な仕様は各専門文書を正とし、この文書には「何が未完了か」「どの文書を残すか」を記載する。

対応環境は Windows 11 25H2以降、日本語環境、x64、下端タスクバーを基本とする。低解像度向けの可変レイアウト対応は行わず、最低720pかつランチャーを収められる作業領域を前提とする。対応範囲外でも例外終了させないための簡単な領域確認は、必要になった場合だけ追加する。

## 2. 修正・検証が必要な項目

### 優先度A: 初回配布前に解消する

| ID | 項目 | 現状と問題 | 完了条件 |
|---|---|---|---|
| R-01 | スタート連動の最終状態統合 | 退出中の再表示取りこぼしと、複数の`SearchHost`から別モニターを選ぶ問題は現行ワークツリーで修正した。ただし、スタートボタンクリック直後にフォーカスがExplorerの`StartButton`へ残り、UI Automationでスタート矩形を取れずWin32フォールバックへ進む瞬間は残り得る。高速なVisible／Hidden通知の揺れも、観測した1パターン以外は未検証。 | クリック元またはスタートを所有するモニターと候補を対応付ける。表示・非表示要求を「最後に要求された状態」へ収束させる。通常操作、連続クリック、退出途中の再表示を複数モニターで反復し、別画面表示・操作不能・点滅がない。 |
| R-02 | 設定検証の強化 | 検証器はレイアウト種別等の一部だけを検証している。項目種別、循環種別、実行後動作の不正な列挙値、ボタンのAction欠落・空対象などを完全には拒否しない。不正JSONを読み込んだ後、設定画面の`First(...)`で例外になる可能性がある。 | 永続化モデルの全列挙値と種類ごとの必須値を検証する。不正値は設定画面を開く前に破損復旧へ回り、空の起動対象は保存時に日本語で指摘する。 |
| R-03 | 未処理例外の診断 | UIスレッド、バックグラウンドTask、終了処理の未処理例外を最後に記録する共通経路がない。環境依存クラッシュで通常ログに原因が残らない可能性がある。 | アプリ、AppDomain、Taskの例外境界を整理し、安全に記録可能な例外を通常ログへ残す。異常終了を隠して継続しない。 |
| R-04 | 設定保存と自動起動の整合性 | 保存時は自動起動のレジストリ変更後にJSONを保存する。JSON保存だけ失敗すると、画面は保存失敗でも自動起動だけ変更済みになる。 | JSON保存と自動起動反映の順序・ロールバック方針を決め、失敗時に利用者へ実際の反映状態を伝える。 |
| R-05 | 配布物の再作成 | 連続スタートクリック、モニター選択、ボタンの実行後表示修正は、既存の配布ZIPより新しいワークツリーにある。 | 修正をコミット後、バージョンを判別できるx64自己完結版を再作成し、別PCでファイル版とログ版が一致することを確認する。 |
| R-13 | 手動表示直後の画面外クリック | `Ctrl+Alt+Space`は`AppWindow.Show(activate: true)`でアクティブ化を要求するが、実際に前面フォーカスを取得できない場合がある。画面外クリックはアクティブから非アクティブへの変化で検出するため、最初から非アクティブだと、ランチャーへ一度フォーカスを合わせるまで画面外クリックで閉じない。これは仕様ではなく不具合である。 | 手動表示後に実際の前面ウィンドウを確認し、必要なら有界な再アクティブ化または画面外クリック検出のフォールバックを行う。ランチャーへ一度も触れず外部ウィンドウをクリックして閉じる操作を10回繰り返し、クリック先を妨げず毎回退出する。 |

### 優先度B: 配布前に仕様を決め、必要なら修正する

| ID | 項目 | 現状と問題 | 推奨方針 |
|---|---|---|---|
| R-06 | コマンドの失敗・再実行という表現 | `Process.Start`に成功した時点で成功扱いとし、batやコマンドの終了コードと実際の画面状態は追跡しない。このため「失敗時は同じ操作を再実行」は、コマンド自身の失敗ではなく起動要求の失敗だけを意味する。 | 初回版では終了待機を追加せず、UIとヘルプを「起動に失敗した場合は同じ操作を再実行」に統一する。終了コード判定は将来機能とする。 |
| R-07 | 詳細ログの個人情報 | パスは匿名化するが、UI Automation要素名にはDiscord等のアカウント名や他アプリの表示内容が含まれ得る。詳細ログは完全匿名ではない。 | スタート判定に不要な要素名を記録しないか伏せ字にする。少なくとも画面と文書で「共有前確認が必要」と明示する。 |
| R-08 | 詳細ログ期限 | トラブルシューティングの適用だけでなく、一般設定を保存するたび有効期限が現在時刻から24時間へ延長される。文書上の「診断設定を保持する」と一致しない。 | 一般設定の保存では既存期限を維持し、診断の専用適用時だけ24時間を再設定する。 |
| R-09 | 設定画面の音声デバイス更新 | 設定画面を開いた時点のキャッシュだけを一覧へ入れる。開いたまま接続・切断しても追加候補と利用可否が更新されない。 | 音声状態変更イベントで一覧を更新するか、明示的な再取得ボタンを設ける。 |
| R-10 | 起動時の同期処理 | 設定読込と一部の音声初期化を同期的に待つため、ストレージ、セキュリティ製品、音声スタックが遅い環境では起動が固まったように見える可能性がある。 | 入力・トレイ・終了を先に利用可能にし、音声列挙等はバックグラウンドで完了させる。起動時間を通常ログで区間計測する。 |
| R-11 | 対応環境の検査 | 文書ではWindows 11 25H2以降、x64、下端タスクバー限定だが、起動時の厳密な判定は未実装で、プロジェクトにはx86／ARM64定義も残る。 | 配布対象はx64だけに固定する。OS／タスクバーを強制終了条件にするか、警告付き非対応とするかを決める。 |
| R-12 | ログローテーション | 2MB判定はロガー初期化時だけで、長時間起動中に2MBを超えても次回起動までローテーションしない。詳細ログの連続記録ではファイルが大きくなり得る。 | 初回個人配布では起動時ローテーションとして明記する。長時間試験で問題になる場合だけ実行中ローテーションを追加する。 |

### 優先度C: 初回配布後でもよい

- 二重起動時に既存プロセスを前面表示する機能。現状は二重常駐を防止して終了するだけである。
- 代替グローバルショートカットを設定画面から変更する機能。
- テーマ、色、アイコン等の外観カスタマイズ。
- コマンドの終了待機、終了コード、タイムアウト、標準出力取得。
- 低解像度向けの可変高さ・専用レイアウト。初回版の対応対象には含めない。

## 3. ドキュメント棚卸し

| 文書 | 判定 | 現状 | 整理方針 |
|---|---|---|---|
| `DESIGN.md` | 維持 | 現行設計の入口として新規作成し、詳細仕様を専門文書へ分離した。 | アプリ全体の現行仕様と文書索引を管理する。詳細値を重複させない。 |
| `archive/INITIAL_DESIGN.md` | 履歴保管 | 要件、構想上のクラス、進捗、テスト、将来案が混在していた旧初期設計書。 | 現行判断には使用せず、設計経緯の確認用にだけ残す。 |
| `START_MENU_INTEGRATION_MAINTENANCE.md` | 維持 | 最も重要な保守資料。UIA／Win32の優先順と過去の競合対策を扱う。 | スタート連動の現行実装についてはこの文書を正とする。連打・退出反転・複数モニター判定の最新仕様と残存リスクを追記する。 |
| `MOTION_SPECIFICATION.md` | 維持 | 旧モーション再設計手順と基準計測から、現在有効なVisual構造、時間、状態、反転、ログ、試験、履歴計測を統合した。 | モーションの現行仕様はこの文書を正とする。 |
| `LOGGING_AND_PRIVACY.md` | 維持・更新 | 保存場所と通常／詳細ログの分離は現行と一致する。期限延長とUI Automation要素名の扱いに実装差・説明不足がある。 | R-07、R-08、R-12とクラッシュログ方針を反映する。 |
| `TEST_CHECKLIST.md` | 維持 | 一人で実施できる日常確認、配布前確認、変更時だけの追加確認へ分けた標準手順。 | 通常のテスト入口として使う。 |
| `PHASE5_6_TEST_PROCEDURE.md` | 維持・参考資料 | 網羅的な試験条件を保持する詳細版。古い合格条件は2026-07-22に修正済み。 | 正式公開前、大きな基盤変更、不具合調査のときだけ必要な範囲を使う。毎回の全項目実施は要求しない。 |
| `VERSIONING.md` | 維持 | 現行の`Version.props`運用と一致する。 | リリース手順書を作る場合は参照する。版番号の唯一の正は`Version.props`とする。 |
| `archive/MOTION_REDESIGN_PROCEDURE.md` | 履歴保管 | 再設計前の手順と当時の技術判断。現在有効な内容はモーション仕様書へ統合済み。 | 現行仕様には使用しない。 |
| `archive/MOTION_BASELINE.md` | 履歴保管 | 再設計前後の単発計測。数値はモーション仕様書の履歴計測へ統合済み。 | 元ログの説明用に残す。 |
| `archive/PHASE0_1_STATUS_2026-07-17.md` | 履歴保管 | 200msポーリング、未実装の状態機械、旧保存先等、現在は解消済みの内容が中心。 | 技術的教訓はスタート保守ガイドへ統合済み。現行判断には使用しない。 |
| `CHANGELOG.md` | 維持 | 利用者影響のある変更履歴として必要。 | 未リリース変更を継続し、配布時に版見出しへ移す。 |

## 4. 今回確認した古い記述

明確に現仕様と食い違う記述は2026-07-22に元文書も修正した。構成の分割や実装変更を伴う項目は残件として管理する。

### 旧`INITIAL_DESIGN.md`

- ボタンにも「実行後」を選択できる記述があった。現行仕様の、ボタンは成功後に閉じ、循環切り替えだけ選択可能という記述へ修正した。
- 「環境変数を扱わない」とあったが、現実装に合わせ、起動対象と作業フォルダーで環境変数を展開すると修正した。
- クラス構成は実装前の構想名が多く、現在のファイル構成を表していない。
- Phase 4の進捗にボタンの実行後動作を含めていたため修正した。
- テスト項目を本文内にも持っており、Phase 5～6手順書と重複している。

旧初期設計書は`archive`へ移し、現行概要と専門文書への索引を持つ`DESIGN.md`へ置き換えた。

### `MOTION_REDESIGN_PROCEDURE.md`

- Win32を先に確認する記述を、フォーカス中のUI Automation矩形を優先し、取れない場合だけWin32へフォールバックする現行順序へ修正した。
- 退出中の反転を新しいWindowsキーだけに限定する記述を、スタート再クリックによる新しいSnapshotも受け入れる内容へ修正した。
- `PublishReadyToRun=false`やARM64ビルドの記録を当時の履歴と明示し、現在はx64、ReadyToRun有効、トリミング無効を正とした。

現在有効な内容と`MOTION_BASELINE.md`の計測値は`MOTION_SPECIFICATION.md`へ統合し、2つの元資料は`archive`へ移した。

### `PHASE5_6_TEST_PROCEDURE.md`

- 低解像度対応を正式な合格条件から外し、最低720pを前提とする内容へ修正した。
- SET-12を、ボタンは成功後に閉じ、循環切り替えだけ実行後動作を選ぶ内容へ修正した。
- SEC-02を、シェルとして実行するコマンド欄と、アプリ／ファイル起動欄のメタ文字規則を分ける内容へ修正した。
- 2MB到達時のローテーションは実行中ではなく次回起動時であると明記した。
- 日常確認と正式リリース判定が同じ文書にあり重かったため、日常用の`TEST_CHECKLIST.md`を新設し、詳細版は必要時だけ使う位置付けへ変更した。

### `START_MENU_INTEGRATION_MAINTENANCE.md`

- コンポーネント表の旧い「Win32優先スキャン」を、UI Automation優先の現行方式へ修正した。
- UI Automationで矩形を得られない最初のクリックにおける、複数`SearchHost`のモニター選択リスクを既知の制約へ追加した。

### `LOGGING_AND_PRIVACY.md`

- 一般設定保存時に詳細ログ期限を維持する設計と、24時間へ延長する現在の実装が一致しないため、設計上の正と実装残件を明記した。
- UI Automation要素名に他アプリのアカウント名等が含まれ得る点を、パス匿名化とは別の制約として強調した。
- 起動中のローテーションと未処理例外ログは未実装である。

## 5. 推奨する最終文書構成

```text
README.md                                   利用者向け概要、対応環境、導入、基本操作
CHANGELOG.md                                配布版ごとの変更
docs/
├─ DESIGN.md                                全体仕様の入口と詳細文書索引
├─ MOTION_SPECIFICATION.md                  モーションの現行仕様
├─ REMAINING_WORK_AND_DOCUMENTATION_AUDIT.md 未完了課題と文書棚卸し
├─ START_MENU_INTEGRATION_MAINTENANCE.md     スタート連動の保守仕様
├─ LOGGING_AND_PRIVACY.md                   ログと共有時の注意
├─ TEST_CHECKLIST.md                        日常・配布前の短い確認
├─ PHASE5_6_TEST_PROCEDURE.md               必要時だけ使う総合試験
├─ VERSIONING.md                            バージョン運用
└─ archive/
   ├─ INITIAL_DESIGN.md
   ├─ PHASE0_1_STATUS_2026-07-17.md
   ├─ MOTION_BASELINE.md
   └─ MOTION_REDESIGN_PROCEDURE.md
```

`README.md`は現在不足している。短い`TEST_CHECKLIST.md`の新設、現行設計書の新設、モーション資料の統合、履歴資料の移動は完了した。

## 6. 整理の実施順

1. R-01～R-05を修正・検証し、現行配布物との差をなくす。
2. ログ仕様に残る実装差を解消する。
3. READMEを作成し、利用者がコード内部の設計書を読まなくても導入できるようにする。
4. x64自己完結版を再作成し、別PCで簡易チェックリストを行う。
